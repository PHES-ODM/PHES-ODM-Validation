name: Test package import

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@f43a0e5 # v3
    - uses: actions/setup-python@7f4fc3e # v4
      with:
        python-version: '3.9'
        cache: 'pip'
    - run: ./inttests/test-import.sh
